<!doctype html>
<html lang="ar" dir="rtl">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>مشغل القصة الصوتية</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .player-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 24px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      padding: 40px;
      max-width: 450px;
      width: 100%;
      text-align: center;
      animation: fadeIn 0.6s ease-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .story-icon {
      font-size: 80px;
      margin-bottom: 20px;
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    h1 {
      color: #667eea;
      font-size: 28px;
      margin-bottom: 10px;
      font-weight: 700;
    }

    .description {
      color: #666;
      font-size: 16px;
      margin-bottom: 30px;
      line-height: 1.6;
    }

    audio {
      width: 100%;
      height: 54px;
      border-radius: 12px;
      outline: none;
      filter: drop-shadow(0 4px 15px rgba(0, 0, 0, 0.15));
    }

    audio::-webkit-media-controls-panel {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 12px;
    }

    .footer {
      margin-top: 25px;
      color: #999;
      font-size: 14px;
    }

    @media (max-width: 600px) {
      .player-container { padding: 30px 25px; }
      h1 { font-size: 24px; }
      .story-icon { font-size: 60px; }
    }
  </style>
 </head>
 <body>
  <div class="player-container">
   <div class="story-icon">🎧</div>
   <h1>قصة اليوم</h1>
   <p class="description">استمتع بالاستماع لهذه القصة الرائعة</p>

   <!-- المشغل الصوتي -->
   <audio controls>
     <!-- استبدل الرابط بالرابط المباشر لملف الصوت في GitHub -->
     <source src="https://raw.githubusercontent.com/username/repo/main/story.mp3" type="audio/mpeg">
     <source src="https://raw.githubusercontent.com/username/repo/main/story.m4a" type="audio/mp4">
     المتصفح لا يدعم تشغيل الصوت.
   </audio>

   <div class="footer">❤️ استمتع بوقتك</div>
  </div>
</body>
</html>
